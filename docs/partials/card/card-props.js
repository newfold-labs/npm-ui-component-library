const cardProps = {
    children: {
        type: 'node',
        description: 'The content of the card.',
        isRequired: true,
    },
    className: {
        type: 'string',
    }
};

export default cardProps;
