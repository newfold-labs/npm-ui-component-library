const textareaProps = {
    rows: {
        type: 'number',
        defaultValue: '2',
    },
    disabled: {
        type: 'bool',
        defaultValue: 'false',
    },
    className: {
        type: 'string',
    }
};

export default textareaProps;
