---

---
import PropsTable from '@site/src/components/PropsTable';
import { Root as NewfoldRoot, TagField } from "@newfold/ui-component-library";
// import Variants from '@site/docs/partials/TagField/_TagField-variants.mdx';

# Tag Field
A simple tag field component.

<NewfoldRoot>
    <TagField
        description="Tag field with a description."
        id="tag-field-1"
        label="Tag field with a label"
        onAddTag={() => {}}
        onRemoveTag={() => {}}
        tags={[
            'Here',
            'are',
            'some',
            'tags'
        ]}
    />
</NewfoldRoot>

---
### Usage
```jsx
import { TagField } from "@newfold/ui-component-library";

<TagField
    description="Tag field with a description."
    id="tag-field-1"
    label="Tag field with a label"
    onAddTag={() => {}}
    onRemoveTag={() => {}}
    tags={[
        'Here',
        'are',
        'some',
        'tags'
    ]}
/>
```
---
### Variants

---
### Props
<PropsTable.Table
    docs={{
        className: {
            type: 'string',
            description: 'Class attribute to add to the element',
            defaultValue: '""',
        },
        description: {
            type: 'string',
            description: 'Form element description',
            defaultValue: '""',
        },
        disabled: {
            type: 'boolean',
            description: 'Disabled flag',
            defaultValue: 'false',
        },
        id: {
            type: 'string',
            description: 'Element id attribute',
            defaultValue: '""',
            isRequired: true,
        },
        label: {
            type: 'string',
            description: 'Form label for field',
            defaultValue: '""',
            isRequired: true,
        },
        labelSuffix: {
            type: 'string',
            description: 'Label suffix',
        },
        tags: {
            type: 'Array of strings',
            description: 'The tags destined for the tagfield. ["Here", "are", "some", "tags"]',
        },
        validation: {
            type: 'Object',
            description: '{ variant: string, message: node }',
        },
    }}
/>
