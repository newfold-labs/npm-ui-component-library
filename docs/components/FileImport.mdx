---

---
import PropsTable from '@site/src/components/PropsTable';
import { Root as NewfoldRoot, FileImport } from "@newfold/ui-component-library";
// import Variants from '@site/docs/partials/FileImport/_FileImport-variants.mdx';

# FileImport
A file import component.

<NewfoldRoot>
    <FileImport
        abortScreenReaderLabel="Abort import"
        dropLabel="or drag and drop"
        feedbackTitle=""
        id="file-import-1"
        name="file-import-1"
        onAbort={() => {}}
        onChange={() => {}}
        progress={40}
        progressMax={100}
        progressMin={0}
        screenReaderLabel="Import a file"
        selectDescription="CSV files only, up to 10MB"
        selectLabel="Select a file"
    />
</NewfoldRoot>

---
### Usage
```jsx
import { FileImport } from "@newfold/ui-component-library";

<FileImport
    abortScreenReaderLabel="Abort import"
    dropLabel="or drag and drop"
    feedbackTitle=""
    id="file-import-1"
    name="file-import-1"
    onAbort={() => {}}
    onChange={() => {}}
    progress={0}
    progressMax={100}
    progressMin={0}
    screenReaderLabel="Import a file"
    selectDescription="CSV files only, up to 10MB"
    selectLabel="Select a file"
/>
```
---
### Variants

---
### Props
<PropsTable.Table docs={{
    abortScreenReaderLabel: {
        type: 'string',
        description: 'Screen reader label for abort',
        isRequired: true,
    },
    children: {
        type: 'node',
    },
    className: {
        type: 'string',
        description: 'Class attribute to add to the element',
        defaultValue: '""',
    },
    dropLabel: {
        type: 'string',
        description: 'Form label for file import',
        defaultValue: '""',
        isRequired: true,
    },
    endStatus: {
        type: 'string ["aborted", "error", "idle", "loading", "selected", "success"]',
        description: 'The status to end the import with (only for testing).',
    },
    feedbackDescription: {
        type: 'string',
        description: 'Feedback description',
    },
    feedbackTitle: {
        type: 'string',
        description: 'Feedback title',
        isRequired: true,
    },
    id: {
        type: 'string',
        description: 'Element id attribute',
        defaultValue: '""',
        isRequired: true,
    },
    name: {
        type: 'string',
        description: 'Element name',
        defaultValue: '""',
        isRequired: true,
    },
    onChange: {
        type: 'func',
        description: 'Handler for when value changes',
        isRequired: true,
    },
    onAbort: {
        type: 'func',
        description: 'Handler for when aborted',
        isRequired: true,
    },
    progress: {
        type: 'number',
        description: 'Progress value',
    },
    progressMax: {
        type: 'number',
        description: 'Progress max value',
    },
    progressMin: {
        type: 'number',
        description: 'Progress min value',
    },
    screenReaderLabel: {
        type: 'string',
        description: 'Screen reader label',
        isRequired: true,
    },
    selectDescription: {
        type: 'string',
        description: 'Select description',
    },
    selectLabel: {
        type: 'string',
        description: 'Form label for file import',
        defaultValue: '""',
        isRequired: true,
    },
    status: {
        type: 'string ["aborted", "error", "idle", "loading", "selected", "success"]',
        description: 'File import status',
        defaultValue: '"idle"',
    },
}}
/>
