---

---
import PropsTable from '@site/src/components/PropsTable';
import { Root as NewfoldRoot, Modal, Button } from "@newfold/ui-component-library";
import CodeBlock from '@theme/CodeBlock';
// import Variants from '@site/docs/partials/Modal/_Modal-variants.mdx';
import { ModalDemo } from '@site/docs/partials/modal';
import ModalDemoCode from '!!raw-loader!../partials/modal/ModalDemo.js';

# Modal
A modal component.

<ModalDemo />

---
### Usage
```jsx
import { Modal } from "@newfold/ui-component-library";
<Modal
    isOpen={ modalOpen }
    onClose={ modalClose }
>
 <p>Modal content</p>
</Modal>

```
<CodeBlock language="jsx">{ModalDemoCode}</CodeBlock>

---
### Variants

---
### Props
<PropsTable.Table
    docs={{
        children: {
            type: 'node',
            isRequired: true,
        },
        className: {
            type: 'string',
            description: 'Class attribute to add to the Modal',
            defaultValue: '""',
        },
        isOpen: {
            type: 'boolean',
            description: 'Open flag',
            defaultValue: 'false',
            isRequired: true,
        },
        onClose: {
            type: 'function',
            description: 'Close callback',
            defaultValue: 'false',
            isRequired: true,
        },
        position: {
            type: 'string [center] [top-center]',
            description: 'Modal position',
            defaultValue: 'center',
        },
        size: {
            type: 'number',
            description: 'Prop for the `Modal.Title` component. [1, 2, 3, 4, 5]',
        },
    }}
/>
